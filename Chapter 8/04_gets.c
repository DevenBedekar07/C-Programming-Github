#include<stdio.h>

int main(){
    
    char st[30];
    
    gets(st);                          // INPUT - deven is nice

    // printf("%s",st);
    puts(st);                         //// Prints the string & places the cursor on the next line 
    printf(" hey");                   // OUTPUT - deven is nice hey
    
    
    return 0;
}